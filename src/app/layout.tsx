import React from 'react';
import type { Metadata, Viewport } from 'next';
import '../styles/tailwind.css';

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
};

export const metadata: Metadata = {
  title: 'RouteDeconstruct — Inside Google Maps Routing',
  description: 'A cinematic scrollytelling experience that deconstructs the Google Maps routing stack — A* algorithm, GNN traffic prediction, and GPS graph theory visualized.',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
